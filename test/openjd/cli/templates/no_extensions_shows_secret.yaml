specificationVersion: "jobtemplate-2023-09"
name: No Extensions Shows Secret
description: Declares no extensions; prints whether SECRET reached the task

steps:
  - name: ShowSecret
    script:
      actions:
        onRun:
          command: python
          args: ["-c", "import os; print('SECRET_IS=' + str(os.environ.get('SECRET')))"]
