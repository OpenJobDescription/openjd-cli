specificationVersion: environment-2023-09
environment:
  name: Env1
  script:
    actions:
      onEnter:
        command: python
        args:
        - -c
        - print('Env1 Enter')
      onExit:
        command: python
        args:
        - -c
        - print('Env1 Exit')
